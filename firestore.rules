rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Open rules for development - CHANGE IN PRODUCTION!
      allow read, write: if true;
    }
  }
}
